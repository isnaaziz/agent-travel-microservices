server:
  port: 8889

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared-configs

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
